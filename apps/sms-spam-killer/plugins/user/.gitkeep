# User Plugins Directory
#
# Place your custom plugins here:
# - .yml/.yaml files for simple pattern matching
# - .js files for complex logic
#
# Example YAML plugin:
# ```yaml
# - name: block-specific-sender
#   description: Block messages from a specific sender
#   match:
#     sender: "\\+1555123456"
#   type: spam
#   confidence: 1.0
# ```
#
# Example JS plugin (create a .js file):
# ```javascript
# export const myClassifier = {
#   id: "my-custom-classifier",
#   classify(message, context) {
#     if (message.content.includes("my-keyword")) {
#       return { type: "custom_type", confidence: 0.9 };
#     }
#     return null;
#   }
# };
# ```
